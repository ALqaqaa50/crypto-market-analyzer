# 🤝 دليل المساهمة

شكرًا لاهتمامك بالمساهمة في Crypto Market Analyzer! نرحب بجميع أنواع المساهمات.

## 📋 كيفية المساهمة

### الإبلاغ عن الأخطاء

إذا وجدت خطأ، يرجى فتح issue مع:
- وصف واضح للمشكلة
- خطوات إعادة إنتاج الخطأ
- السلوك المتوقع مقابل السلوك الفعلي
- البيئة (نظام التشغيل، إصدار Python، إلخ)

### اقتراح ميزات جديدة

لاقتراح ميزة جديدة:
1. تحقق من عدم وجود اقتراح مماثل في Issues
2. افتح issue جديد مع تسمية "enhancement"
3. اشرح الميزة المقترحة وحالات الاستخدام
4. ناقش التنفيذ المحتمل

### المساهمة بالكود

1. **Fork المستودع**
   ```bash
   git clone https://github.com/YOUR_USERNAME/crypto-market-analyzer.git
   cd crypto-market-analyzer
   ```

2. **إنشاء فرع جديد**
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **إعداد بيئة التطوير**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   pip install -r requirements-dev.txt  # إذا كان موجودًا
   ```

4. **إجراء التغييرات**
   - اتبع أسلوب الكود الموجود
   - أضف تعليقات واضحة
   - اكتب اختبارات للميزات الجديدة

5. **تشغيل الاختبارات**
   ```bash
   pytest
   ```

6. **Commit التغييرات**
   ```bash
   git add .
   git commit -m "Add: وصف واضح للتغيير"
   ```

7. **Push إلى GitHub**
   ```bash
   git push origin feature/amazing-feature
   ```

8. **فتح Pull Request**
   - اشرح التغييرات بوضوح
   - اربط أي issues ذات صلة
   - انتظر المراجعة

## 📝 معايير الكود

### أسلوب Python

- اتبع [PEP 8](https://www.python.org/dev/peps/pep-0008/)
- استخدم type hints حيثما أمكن
- أقصى طول للسطر: 100 حرف
- استخدم docstrings لجميع الدوال والكلاسات

### التسميات

```python
# Classes: PascalCase
class WhaleDetector:
    pass

# Functions/methods: snake_case
def calculate_vwap():
    pass

# Constants: UPPER_SNAKE_CASE
MAX_RETRIES = 3

# Private: prefix with _
def _internal_method():
    pass
```

### التوثيق

```python
def calculate_indicator(data: List[float], period: int = 14) -> float:
    """
    حساب المؤشر الفني.
    
    Args:
        data: قائمة بيانات الأسعار
        period: الفترة الزمنية للحساب
        
    Returns:
        قيمة المؤشر المحسوبة
        
    Raises:
        ValueError: إذا كانت البيانات غير كافية
    """
    pass
```

### الاختبارات

- اكتب اختبارات لجميع الميزات الجديدة
- استخدم pytest
- استهدف تغطية كود 80%+

```python
def test_whale_detector():
    detector = WhaleDetector(threshold=100000)
    result = detector.detect(mock_trades)
    assert len(result) > 0
```

## 🔍 عملية المراجعة

1. سيقوم أحد المشرفين بمراجعة PR الخاص بك
2. قد يُطلب منك إجراء تغييرات
3. بعد الموافقة، سيتم دمج PR
4. سيتم إضافتك إلى قائمة المساهمين

## 🎯 أولويات المساهمة

نحن نبحث بشكل خاص عن مساهمات في:

- **الاختبارات**: زيادة تغطية الاختبارات
- **التوثيق**: تحسين الشروحات والأمثلة
- **الأداء**: تحسينات الأداء
- **الميزات**: استراتيجيات تداول جديدة، مؤشرات فنية
- **إصلاح الأخطاء**: أي أخطاء موجودة

## 📧 التواصل

- افتح issue للمناقشات العامة
- استخدم Pull Requests للكود
- للأسئلة السريعة، استخدم Discussions

## 📜 ميثاق السلوك

- كن محترمًا ومهذبًا
- قدّر وجهات النظر المختلفة
- اقبل النقد البناء
- ركز على ما هو أفضل للمجتمع

شكرًا لمساهمتك! 🙏
